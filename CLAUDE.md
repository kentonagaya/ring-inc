# CLAUDE.md — ring-inc.com（株式会社リング）サイト保守プロジェクト

このファイルは、WordPressカスタムテーマ `site-theme` の保守・改修における設計方針、これまでの実装内容、およびその判断理由をまとめたものです。Claude（およびClaude Code）が本リポジトリで作業する際の前提知識として参照してください。

※ 2026-08-02: Downloads にあった旧版が消失していたため、当日時点の進捗を反映してリポジトリ直下に復元。

---

## 1. プロジェクト概要

- **サイト**: https://ring-inc.com/（株式会社リングのコーポレートサイト）
- **構成**: WordPress + カスタムテーマ `wp-content/themes/site-theme` + All in One SEO (AIOSEO) プラグイン（title / description / OG / canonical / JSON-LD を出力。2026-08時点 v4.9.10）
- **事業**: 高齢者トータルサポート / 不動産紹介 / 人材紹介 / 就労継続支援事業所（B型）すまいる の4事業
- **経緯**: 初版から約7年、継ぎ足しで更新されてきたため、テーマ内に新旧2世代のシステム（ヘッダー・フォーム等）が併存している
- **デプロイ**: FTPでテーマファイルをアップロード
- **管理**: ローカルGitリポジトリ `/Users/kentonagaya/ring`（WordPress一式を含む）
- **注意**: ローカルリポジトリの `wp-content/plugins` は本番と乖離している（AIOSEO・gtm4wp 等は本番のみに存在。2026-08-02確認）。プラグインの調査はライブサイトのHTML出力か公式ドキュメントで行うこと

---

## 2. SCSSアーキテクチャ（2026年再構築済み）

### 現在の正規構成（3ファイル）

```
scss/
├── style.scss      # エントリポイント。フォントの @import のみ
├── _settings.scss  # 色変数7つ（下記）
└── _main.scss      # 全スタイル本体。色リテラルは変数に置換済み
```

色変数の定義（`_settings.scss`）:

| 変数 | 値 |
|---|---|
| `$accent-color` | `#0278e2` |
| `$sub-color` | `#51b4d8` |
| `$light-color` | `#f2f2f2` |
| `$body-color` | `#fafafa` |
| `$text-color` | `#333` |
| `$deep-color` | `#ddd` |
| `$main-color` | `#000` |

### なぜ全面再構築したか（重要な経緯）

**旧SCSSはライブの `css/style.css` と大きく乖離しており、そのままコンパイルするとサイトが壊れる状態だった**（色値・フォントサイズ・画像パス・クラス名の不一致）。そのため以下の判断をした:

1. **ライブの `css/style.css` を正（authoritative）とする**。SCSSではなく、実際に配信されているCSSがサイトの真実である
2. SCSSをゼロから再構築し、コンパイル結果がライブCSSと一致することを検証した
3. 検証は**セマンティック差分（PostCSSによる2パス比較: 有効カスケード + 宣言タプル集合）**で行い、約2,320のセレクタ×プロパティ組でゼロ差分を確認済み
4. 旧SCSSパーシャル群（reset / structures / modules / contents / 旧settings）は削除対象

### SCSS作業の絶対ルール

- **SCSSを変更したら必ずCSSにコンパイルして反映する**（SCSSだけ直してCSSが古いまま、が過去の乖離の原因）
- コンパイラは dart-sass。`@import` の非推奨警告が出るが、**このプロジェクトでは無害**なので対応不要
- リファクタリング後の動作同一性検証にはPostCSSのセマンティック差分を使う
- CSSの整形にはPrettierを使用

---

## 3. ページテンプレートの方針

### `the_content()` パターンへの移行

固定ページの本文をPHPテンプレート直書きからWordPressエディタ管理に移行している。移行済みは3ページ:

| ページ | テンプレート |
|---|---|
| 高齢者トータルサポート | `page-senior-support.php` |
| 不動産紹介 | `page-realestate.php` |
| 人材紹介 | `page-resources.php` |

移行時のルール:

- PHP式は静的HTMLに変換し、画像URLは**絶対URL**にする
- **HTMLコメントは必ず除去する**（残すとブロックエディタがブロックを分断してしまう）
- **デプロイ順序**: ①WordPressエディタのカスタムHTMLブロックに本文を貼り付け → ②その後にPHPテンプレートをFTPアップロード。この順序を守ることで公開中のページが一瞬も壊れない

### 理由

本文をエディタ管理にすることで、テキスト修正のたびにFTPデプロイが不要になり、非エンジニアでも更新できる運用に近づけるため。

---

## 4. URL・命名の変更履歴

### 高齢者トータルサポート: `/nursinghome` → `/senior-support`

実施内容一式:

- テンプレートを `page-senior-support.php` にリネーム
- `_main.scss` 内の `#nursinghome-body` → `#senior-support-body`（11箇所）
- サイトマップ・トップページのリンク更新
- WordPressのスラッグ変更
- **301リダイレクト設定**（旧URL→新URL）

理由: 「nursinghome（老人ホーム）」は事業実態（施設紹介を含むトータルサポート）より狭い名称であり、英語圏の語感としても不正確なため。

補足（2026-08時点のGSC所見）: 旧URL `/nursinghome` はまだインデックスに残存し新URLと並存中。301は設定済みなので時間経過で解消見込み。**2026-08-02: GSCのURL検査で新URLの登録リクエスト実施済み。**

### その他の命名変更

- 人材紹介の英語ラベル: `RECRUITMENT` → `Recruitment Services`（採用情報ページとの混同回避）

---

## 5. フッターの実装変更

- 本社と営業所を**横並び表示**に変更
- 両セクションから営業時間の表示を削除
- フリーダイヤル変数を `$phone_num_2` → `$phone_num_f` にリネーム（「2番目の電話番号」ではなく「フリーダイヤル」という役割を変数名で表すため）

---

## 6. 作業時の約束事（ワークフロー）

1. **SCSSとライブCSSが同期している前提を置かない**。編集前に必ず現状を確認する
2. **どのファイルを編集するか、着手前に確認する**（過去にファイル特定の誤りで手戻りが発生している）
3. 作業は段階的に進め、**フェーズ間に明示的な検証ステップを挟む**
4. ローカルリポジトリ `/Users/kentonagaya/ring` をプロジェクトナレッジとして参照し、ファイルの現状はリポジトリで確認する

---

## 6.5 SEOタグの出力機構（2026-08-02 調査で確定・重要）

- ライブの `<head>` を出力しているのは**旧世代の `assets/common/html-header.php`**（`header.php` は `<body>` 以降のみを出力）
- meta description のサイト共通フォールバックは WP 管理画面「設定 > 一般」の**キャッチフレーズ**（`bloginfo('description')`）
- **AIOSEO** が `<title>`・description・canonical・OG・Twitter・JSON-LD（Organization / WebSite / BreadcrumbList）を wp_head 経由で出力。トップページではテーマと AIOSEO が description を**重複出力**している（文言はどちらもキャッチフレーズ由来）
- `inc/functions/generate-description.php` / `generate-title.php` / `inc/head/mod-head.php` は functions.php から読み込まれていない**死にコード**（新世代システムの残骸）。ACF `page-meta_description` の仕組みは動いていない
- 各ページ個別 description は AIOSEO のページ編集メタボックスで設定可（ただしテーマ側フォールバックと重複する点に注意）

---

## 7. 既知の課題バックログ（2026-08-02 サイト総合分析より）

以下は分析で確認済みの課題。優先度順。

### 即対応(管理画面・小修正で完結)

- [x] サイト共通の title / meta description を4事業の文言へ書き換え → **2026-08-02 完了・ライブ反映確認済み**（キャッチフレーズ + AIOSEO ホームページ設定を変更）。og:site_name も **2026-08-02 解消・ライブ確認済み**（出力元は AIOSEO → ソーシャルネットワーク → Facebook タブ → 「サイト名」。検索の外観ではない点に注意）
- [ ] 各固定ページの個別description未設定 → 実際に使えるのは **AIOSEO の各ページ編集画面のメタボックス**での入力（ACF/generate-description.php の仕組みは死にコード）。テーマ旧ヘッダーもキャッチフレーズを description 出力するため、AIOSEO側で個別設定するとタグが重複する点に注意
- [x] 誤記「専門スタッフが**在住**」→「**常駐**」 → **2026-08-02 完了・ライブ確認済み**（トップは `index.php`、senior-support は管理画面のカスタムHTMLブロックで修正）
- [x] 会社概要「ご挨拶」の「3つの事業」→ 4事業に更新 → **2026-08-02 完了・ライブ確認済み**。【高齢者トータルサポート】【不動産紹介】【人材紹介】【就労継続支援】の4事業表記に
- [x] 会社概要「主な業務内容」テーブルの改行崩れ修正 + 就労継続支援事業の追記 → **2026-08-02 完了・ライブ確認済み**。「就労継続支援事業所（B型）「すまいる」の運営」を追記。**改行崩れはPC幅のレイアウト計測で再現せず**（全行が意図した行数で描画。過去の更新で解消済みとみられる）。実機SPで崩れが見えたら再調査
- [x] トップページの事業カードに「すまいる」を追加 → **2026-08-02 完了・ライブ確認済み**。`index.php` の事業案内を `mod-clm3`→`mod-clm4` の4カード化。画像は `wp-content/uploads/2026/06/IMG_0499.jpeg`（すまいる外観・既存カードと同じ4:3）を絶対URLで参照、リンク先 `/workshop/`
- [x] `footer.php` のページトップリンク `<a href=".container">` は壊れたリンク → **2026-08-02 修正・デプロイ・ライブ確認済み**。`href="#"` に変更（base.js の `.pagetop` クリックハンドラが `return false` でスクロール処理するため、href はJS失敗時のフォールバック）
- [x] viewport の `maximum-scale=1.0, minimum-scale=1.0` を削除（スマホのピンチ拡大禁止はアクセシビリティ上NG。高齢者向けサイトでは特に）→ **2026-08-02 完了・ライブ確認済み**
- [x] 表記統一: 「営業部」（会社概要）/「営業所」（フッター）→ **2026-08-02 完了・ライブ確認済み**。会社概要の2箇所（概要テーブル・アクセスマップ見出し）を「営業所」に統一

### 中期（テーマ改修）

- [ ] ~~JSON-LD構造化データが皆無~~ → **訂正（2026-08-02）**: AIOSEO が `Organization` / `WebSite` / `BreadcrumbList` を自動出力済み。未対応は `LocalBusiness`（すまいる: 住所・営業時間・座標）の追加のみ
- [x] meta keywords の出力削除（SEO効果なし、競合への情報漏洩のみ）→ **2026-08-02 修正・デプロイ・ライブ確認済み**。`html-header.php` の出力ブロック削除 + `site-config.php` の `$meta_keywords` を空に（comingsoon.php が参照するため変数自体は残置）
- [ ] og:url が下層ページでトップURLを指すケースあり（新旧ヘッダー併存が原因）。ヘッダーを新世代に一本化
- [x] 複数h1の解消 → **2026-08-02 修正済み（FTPデプロイ待ち: 13ファイル）**。設計: 下層は `mod-pagetitle` のページタイトル帯がh1、ヘッダーのサイト名はトップのみh1（下層はp）、記事タイトル・セクション見出しはh2（`single-works.php` が先行していた h2.heading01 規約に統一）。CSSは全てクラスセレクタのため見た目の変化なし
- [ ] `/staff` `/recruit` が孤立ページ。グローバルナビに追加、「個人情報保護方針」はフッターへ（注: `/workshop`「就労支援施設」はナビ掲載済みを2026-08-02に確認）
- [x] 未使用ファイルの廃棄 → **2026-08-02 リポジトリから削除済み**: `comingsoon.php` + `css/comingsoon/`、`form.class-cn.php` / `form.class-en.php`（使用中の `form.class.php` は残置）、`pages/sample/`、`$meta_keywords` 変数。**サーバー側の削除も2026-08-02完了（404確認済み）**。`conf/sitemap.php`（未ロードの新世代ナビ設定）等の残りの死にコードはヘッダー一本化のタスクで一括処理
- [ ] フォームシステム2系統（`page-contact.php` 旧系 / `pages/contact/` 新系）の一本化。**触る前にどちらが本番で使われているか必ず確認**
- [ ] ローディング演出（Now Loading）の削除、フルセット `fontawesome-all.min.js` のサブセット化、jQuery系ライブラリ（owl / slick / instafeed）の整理

### コンテンツ（SEO戦略）

- GSC所見: 流入はほぼ100%指名検索。唯一の非指名成長シードは**すまいる関連の地域クエリ**（「就労支援ｂ型 大阪市鶴見区」で1〜2位）。ここへのコンテンツ投資（工賃・1日の流れ・手続き・イベント報告）が最もROIが高い
- 「株式会社ring 怪しい/評判」系クエリが存在。実績数値・スタッフ紹介・お客様の声・FAQ・許認可の明示で信頼コンテンツを整備する

---

## 8. 使用ツール

| 用途 | ツール |
|---|---|
| SCSSコンパイル | dart-sass |
| CSS動作同一性の検証 | PostCSS（セマンティック差分: 有効カスケード + 宣言タプル比較） |
| CSS整形 | Prettier |
| デプロイ | FTP |
| コード参照 | ローカルGitリポジトリ `/Users/kentonagaya/ring` |
